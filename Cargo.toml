[workspace]
members = [
    "programs/*",
    "indexer",
    "relayer",
    "census-service",
    "smt-circom",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = true
codegen-units = 1
